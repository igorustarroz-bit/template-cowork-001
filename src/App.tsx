/**
 * Playground de desarrollo (npm run dev).
 * La documentación oficial del design system vive en Storybook.
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>Euro6000 · Design System</h1>
      <p>
        Playground de desarrollo. Ejecuta <code>npm run storybook</code> para
        ver la documentación de tokens, componentes y módulos.
      </p>
    </main>
  );
}
